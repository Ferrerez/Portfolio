function reajustar () {
    var nome = prompt('Qual o nome do funcionário?')
    var salario = prompt(`Qual é o salário de ${nome}?`)
    var reajus = prompt(`O salário de ${nome} vai ser ajustado em qual porcentagem?`)

}